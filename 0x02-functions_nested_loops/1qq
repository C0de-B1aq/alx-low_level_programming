#include "main.h"

/**
 * print_alphabet_x10 - 'prints the alphabet 10 times'
 *
 */

void print_alphabet_x10(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		print_alphabet_x10(i(*10));

	_putchar('\n')
}
